package jchopp.strategies.defaults;

import jchopp.objects.logical.standards.LogicalObject;
import jchopp.strategies.interfaces.TickingStrategy;

public class DefaultTickingStrategy implements TickingStrategy {
    @Override
    public void tick(LogicalObject object) {
        // nothing
    }
}
